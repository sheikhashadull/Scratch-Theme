<a class="btn" href="{{ link }}">{{ text | default: 'Take the Quiz' }}</a>

{% style %}

.btn {
    display: inline-block;
    background-color: {{ btncolor }};
    padding: {{btn_paddingx}}px {{btn_paddingy}}px;
    border-radius: 10px;
}
{% endstyle %}