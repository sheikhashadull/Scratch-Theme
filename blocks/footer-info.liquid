{{ block.settings.title }}

{% schema %}
{
    "name": "Info",
    "settings": [
        {
            "type": "text",
            "id": "title",
            "label": "Title",
            "default": "GET 10% OFF YOUR FIRST ORDER WHEN SIGNING UP TO OUR NEWSLETTER"
        },
    ],
    "presets": [
        {
            "name": "Info"
        }
    ]
}
{% endschema %}