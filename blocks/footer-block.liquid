{{ block.settings.title }}


{% schema %}
    {
        "name": "Company Info",
        "tag": null,
        "settings": [
            {
                "type": "text",
                "id": "title",
                "label": "Title",
                "default": "GET 10% OFF YOUR FIRST ORDER WHEN SIGNING UP TO OUR NEWSLETTER"
            }
        ],
        "presets": [
            {
                "name": "Company Info"
            }
        ]
    }
{% endschema %}