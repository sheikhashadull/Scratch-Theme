{{ block.settings.title }}
{{ block.settings.link_title }}
{{ block.settings.footer_link }}


{% schema %}
{
    "name": "Info",
    "settings": [
        {
            "type": "text",
            "id": "title",
            "label": "Title",
            "default": "GET 10% OFF YOUR FIRST ORDER WHEN SIGNING UP TO OUR NEWSLETTER"
        },
        {
            "type": "text",
            "id": "link_title",
            "label": "Link Title",
            "default": "Customer Service"
        },
        {
            "type": "link_list",
            "id": "footer_link",
            "label": "Links"
        }
    ],
    "presets": [
        {
            "name": "Info"
        }
    ]
}
{% endschema %}