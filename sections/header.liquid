<section class="color-{{ section.settings.color_scheme }} gradient">
  <h1>Welcome</h1>
<a class="btn">Shop now</a>
<a class="stock-btn">hi</a>
</section>

<style>
    section {
     background: var(--bg);
     color: var(--text);
    }

    .header_section .btn {
    padding: 10px 20px;
    background: var(--btn);
    border: 5px solid var(--btn-stock);
    box-shadow: 0 10px 30px var(--color-shadow);
    }
}


</style>


{% schema %}
    {
        "name": "Header",
        "class": "header_section",
        "settings": [
            {
                "type": "color_scheme",
                "id": "color_scheme",
                "label": "Color Scheme"
            }
        ]
    }
{% endschema %}